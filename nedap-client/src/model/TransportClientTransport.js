/*
 * Nedap Ons API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 0.0.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.77
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';
import HourType from './HourType';

/**
 * The TransportClientTransport model module.
 * @module model/TransportClientTransport
 * @version 0.0.0
 */
export default class TransportClientTransport {
  /**
   * Constructs a new <code>TransportClientTransport</code>.
   * ClientTransport the transport details for a client, financed in the WLZ, on a certain date
   * @alias module:model/TransportClientTransport
   * @class
   * @param clientId {Number} Id of {@link com.nedap.healthcare.domain.Client} for which these details are valid
   * @param _date {Date} Date for which the transport details for the given client is retrieved
   * @param individualTransport {Boolean} Is individual transport needed for this client at the given date
   * @param hasWeelchairAssignment {Boolean} Has this client a wheelchair assigned at the given date
   * @param wlz {Boolean} Receives this client WLZ care at the given date
   * @param hourTypes {Array.<module:model/HourType>} Set of all {@link HourType} instances that can be used for this client
   */
  constructor(clientId, _date, individualTransport, hasWeelchairAssignment, wlz, hourTypes) {
    this.clientId = clientId;
    this._date = _date;
    this.individualTransport = individualTransport;
    this.hasWeelchairAssignment = hasWeelchairAssignment;
    this.wlz = wlz;
    this.hourTypes = hourTypes;
  }

  /**
   * Constructs a <code>TransportClientTransport</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/TransportClientTransport} obj Optional instance to populate.
   * @return {module:model/TransportClientTransport} The populated <code>TransportClientTransport</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new TransportClientTransport();
      if (data.hasOwnProperty('id'))
        obj.id = ApiClient.convertToType(data['id'], 'Number');
      if (data.hasOwnProperty('clientId'))
        obj.clientId = ApiClient.convertToType(data['clientId'], 'Number');
      if (data.hasOwnProperty('date'))
        obj._date = ApiClient.convertToType(data['date'], 'Date');
      if (data.hasOwnProperty('individualTransport'))
        obj.individualTransport = ApiClient.convertToType(data['individualTransport'], 'Boolean');
      if (data.hasOwnProperty('hasWeelchairAssignment'))
        obj.hasWeelchairAssignment = ApiClient.convertToType(data['hasWeelchairAssignment'], 'Boolean');
      if (data.hasOwnProperty('wlz'))
        obj.wlz = ApiClient.convertToType(data['wlz'], 'Boolean');
      if (data.hasOwnProperty('contractedTransport'))
        obj.contractedTransport = ApiClient.convertToType(data['contractedTransport'], 'Boolean');
      if (data.hasOwnProperty('hourTypes'))
        obj.hourTypes = ApiClient.convertToType(data['hourTypes'], [HourType]);
      if (data.hasOwnProperty('createdAt'))
        obj.createdAt = ApiClient.convertToType(data['createdAt'], 'Date');
      if (data.hasOwnProperty('updatedAt'))
        obj.updatedAt = ApiClient.convertToType(data['updatedAt'], 'Date');
    }
    return obj;
  }
}

/**
 * @member {Number} id
 */
TransportClientTransport.prototype.id = undefined;

/**
 * Id of {@link com.nedap.healthcare.domain.Client} for which these details are valid
 * @member {Number} clientId
 */
TransportClientTransport.prototype.clientId = undefined;

/**
 * Date for which the transport details for the given client is retrieved
 * @member {Date} _date
 */
TransportClientTransport.prototype._date = undefined;

/**
 * Is individual transport needed for this client at the given date
 * @member {Boolean} individualTransport
 * @default false
 */
TransportClientTransport.prototype.individualTransport = false;

/**
 * Has this client a wheelchair assigned at the given date
 * @member {Boolean} hasWeelchairAssignment
 * @default false
 */
TransportClientTransport.prototype.hasWeelchairAssignment = false;

/**
 * Receives this client WLZ care at the given date
 * @member {Boolean} wlz
 * @default false
 */
TransportClientTransport.prototype.wlz = false;

/**
 * Indicates whether the client uses contracted transport
 * @member {Boolean} contractedTransport
 * @default false
 */
TransportClientTransport.prototype.contractedTransport = false;

/**
 * Set of all {@link HourType} instances that can be used for this client
 * @member {Array.<module:model/HourType>} hourTypes
 */
TransportClientTransport.prototype.hourTypes = undefined;

/**
 * @member {Date} createdAt
 */
TransportClientTransport.prototype.createdAt = undefined;

/**
 * @member {Date} updatedAt
 */
TransportClientTransport.prototype.updatedAt = undefined;

