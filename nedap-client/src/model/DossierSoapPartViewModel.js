/*
 * Nedap Ons API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 0.0.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.77
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';
import DossierReportAuthorizationsViewModel from './DossierReportAuthorizationsViewModel';

/**
 * The DossierSoapPartViewModel model module.
 * @module model/DossierSoapPartViewModel
 * @version 0.0.0
 */
export default class DossierSoapPartViewModel {
  /**
   * Constructs a new <code>DossierSoapPartViewModel</code>.
   * Individual part of the SOAP report (subjective/objective/assessment/plan)
   * @alias module:model/DossierSoapPartViewModel
   * @class
   * @param label {String} Dutch or English language dependent name of the SOAP part
   * @param visible {Boolean} Set to `true` if the employee is allowed to see this SOAP part.  If set to `false` then the `comment` attribute is empty but list of `authorizations` is still visible.  It means that an employee cannot see the comment but they still can see list of expertise profiles and groups who are allowed to see the comment. 
   * @param confidential {Boolean} When set to `true` then this report is not public and can only be seen by employees with proper level of authorization (see the `authorizations` property in this model) 
   */
  constructor(label, visible, confidential) {
    this.label = label;
    this.visible = visible;
    this.confidential = confidential;
  }

  /**
   * Constructs a <code>DossierSoapPartViewModel</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/DossierSoapPartViewModel} obj Optional instance to populate.
   * @return {module:model/DossierSoapPartViewModel} The populated <code>DossierSoapPartViewModel</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new DossierSoapPartViewModel();
      if (data.hasOwnProperty('label'))
        obj.label = ApiClient.convertToType(data['label'], 'String');
      if (data.hasOwnProperty('comment'))
        obj.comment = ApiClient.convertToType(data['comment'], 'String');
      if (data.hasOwnProperty('visible'))
        obj.visible = ApiClient.convertToType(data['visible'], 'Boolean');
      if (data.hasOwnProperty('confidential'))
        obj.confidential = ApiClient.convertToType(data['confidential'], 'Boolean');
      if (data.hasOwnProperty('authorizations'))
        obj.authorizations = DossierReportAuthorizationsViewModel.constructFromObject(data['authorizations']);
    }
    return obj;
  }
}

/**
 * Dutch or English language dependent name of the SOAP part
 * @member {String} label
 */
DossierSoapPartViewModel.prototype.label = undefined;

/**
 * Text of the SOAP part. It is present only if the current employee is allowed to see this SOAP part.
 * @member {String} comment
 */
DossierSoapPartViewModel.prototype.comment = undefined;

/**
 * Set to `true` if the employee is allowed to see this SOAP part.  If set to `false` then the `comment` attribute is empty but list of `authorizations` is still visible.  It means that an employee cannot see the comment but they still can see list of expertise profiles and groups who are allowed to see the comment. 
 * @member {Boolean} visible
 */
DossierSoapPartViewModel.prototype.visible = undefined;

/**
 * When set to `true` then this report is not public and can only be seen by employees with proper level of authorization (see the `authorizations` property in this model) 
 * @member {Boolean} confidential
 */
DossierSoapPartViewModel.prototype.confidential = undefined;

/**
 * @member {module:model/DossierReportAuthorizationsViewModel} authorizations
 */
DossierSoapPartViewModel.prototype.authorizations = undefined;

