/*
 * Nedap Ons API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 0.0.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.77
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';
import DepartureLocation from './DepartureLocation';
import ExpenseExpenseDistance from './ExpenseExpenseDistance';

/**
 * The ExpenseExpense model module.
 * @module model/ExpenseExpense
 * @version 0.0.0
 */
export default class ExpenseExpense {
  /**
   * Constructs a new <code>ExpenseExpense</code>.
   * An expense is a value of some kind (determined) by its type, that an employee will be compensated for (usually via her payroll). &lt;p&gt; Expenses can either be manual or automatic. Manual expenses will simply consist of an {@link #amount}. Automatic expenses are based on the {@link #calculatedAmount} that, combined with a {@link #deviation}, leads to the expense {@link #amount}. &lt;/p&gt;
   * @alias module:model/ExpenseExpense
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>ExpenseExpense</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/ExpenseExpense} obj Optional instance to populate.
   * @return {module:model/ExpenseExpense} The populated <code>ExpenseExpense</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new ExpenseExpense();
      if (data.hasOwnProperty('id'))
        obj.id = ApiClient.convertToType(data['id'], 'Number');
      if (data.hasOwnProperty('expenseDate'))
        obj.expenseDate = ApiClient.convertToType(data['expenseDate'], 'Date');
      if (data.hasOwnProperty('verifyDate'))
        obj.verifyDate = ApiClient.convertToType(data['verifyDate'], 'Date');
      if (data.hasOwnProperty('expenseTypeObjectId'))
        obj.expenseTypeObjectId = ApiClient.convertToType(data['expenseTypeObjectId'], 'Number');
      if (data.hasOwnProperty('employeeObjectId'))
        obj.employeeObjectId = ApiClient.convertToType(data['employeeObjectId'], 'Number');
      if (data.hasOwnProperty('amount'))
        obj.amount = ApiClient.convertToType(data['amount'], 'Number');
      if (data.hasOwnProperty('calculatedAmount'))
        obj.calculatedAmount = ApiClient.convertToType(data['calculatedAmount'], 'Number');
      if (data.hasOwnProperty('deviation'))
        obj.deviation = ApiClient.convertToType(data['deviation'], 'Number');
      if (data.hasOwnProperty('description'))
        obj.description = ApiClient.convertToType(data['description'], 'String');
      if (data.hasOwnProperty('expenseDistances'))
        obj.expenseDistances = ApiClient.convertToType(data['expenseDistances'], [ExpenseExpenseDistance]);
      if (data.hasOwnProperty('sourceType'))
        obj.sourceType = ApiClient.convertToType(data['sourceType'], 'Number');
      if (data.hasOwnProperty('departureLocation'))
        obj.departureLocation = DepartureLocation.constructFromObject(data['departureLocation']);
      if (data.hasOwnProperty('departureClusterObjectId'))
        obj.departureClusterObjectId = ApiClient.convertToType(data['departureClusterObjectId'], 'Number');
    }
    return obj;
  }
}

/**
 * @member {Number} id
 */
ExpenseExpense.prototype.id = undefined;

/**
 * The date on which the expense was made (or booked)
 * @member {Date} expenseDate
 */
ExpenseExpense.prototype.expenseDate = undefined;

/**
 * The date on which the expense was verified (through an approval process)
 * @member {Date} verifyDate
 */
ExpenseExpense.prototype.verifyDate = undefined;

/**
 * Reference to the expense type that underlies the expense. This id is guaranteed not to change within an customer environment. There is no API object available.
 * @member {Number} expenseTypeObjectId
 */
ExpenseExpense.prototype.expenseTypeObjectId = undefined;

/**
 * Id of the employee to which the expense belongs.
 * @member {Number} employeeObjectId
 */
ExpenseExpense.prototype.employeeObjectId = undefined;

/**
 * The expense amount. This can kilometers, hours, or money. <p> Either an abosulte value or the sum of calculated value and deviation. </p>
 * @member {Number} amount
 */
ExpenseExpense.prototype.amount = undefined;

/**
 * An expense can be calculated automatically from other information. In such cases the calculated amount repesents the sum of this information.
 * @member {Number} calculatedAmount
 */
ExpenseExpense.prototype.calculatedAmount = undefined;

/**
 * Employees are given the option to register a deviation to the calculated amount.
 * @member {Number} deviation
 */
ExpenseExpense.prototype.deviation = undefined;

/**
 * Employees may need to give a description of the expense.
 * @member {String} description
 */
ExpenseExpense.prototype.description = undefined;

/**
 * For expenses that represent a distance, there may be {@link ExpenseDistance}s to further account the expense. <p>Distances are ordered based on the underlying work (e.g. client visits)</p>
 * @member {Array.<module:model/ExpenseExpenseDistance>} expenseDistances
 */
ExpenseExpense.prototype.expenseDistances = undefined;

/**
 * @member {Number} sourceType
 */
ExpenseExpense.prototype.sourceType = undefined;

/**
 * @member {module:model/DepartureLocation} departureLocation
 */
ExpenseExpense.prototype.departureLocation = undefined;

/**
 * @member {Number} departureClusterObjectId
 */
ExpenseExpense.prototype.departureClusterObjectId = undefined;

