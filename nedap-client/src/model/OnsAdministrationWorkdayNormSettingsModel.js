/*
 * Nedap Ons API
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 0.0.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.77
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';

/**
 * The OnsAdministrationWorkdayNormSettingsModel model module.
 * @module model/OnsAdministrationWorkdayNormSettingsModel
 * @version 0.0.0
 */
export default class OnsAdministrationWorkdayNormSettingsModel {
  /**
   * Constructs a new <code>OnsAdministrationWorkdayNormSettingsModel</code>.
   * Workday norm settings model. This model describes the maximum workdays a given employee has to work per week during a given measurement period. 
   * @alias module:model/OnsAdministrationWorkdayNormSettingsModel
   * @class
   * @param employeeObjectId {Number} The employee object id
   * @param startDate {Date} The start date of the period during which this setting is valid
   */
  constructor(employeeObjectId, startDate) {
    this.employeeObjectId = employeeObjectId;
    this.startDate = startDate;
  }

  /**
   * Constructs a <code>OnsAdministrationWorkdayNormSettingsModel</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/OnsAdministrationWorkdayNormSettingsModel} obj Optional instance to populate.
   * @return {module:model/OnsAdministrationWorkdayNormSettingsModel} The populated <code>OnsAdministrationWorkdayNormSettingsModel</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new OnsAdministrationWorkdayNormSettingsModel();
      if (data.hasOwnProperty('claDefault'))
        obj.claDefault = ApiClient.convertToType(data['claDefault'], 'Boolean');
      if (data.hasOwnProperty('employeeObjectId'))
        obj.employeeObjectId = ApiClient.convertToType(data['employeeObjectId'], 'Number');
      if (data.hasOwnProperty('endDate'))
        obj.endDate = ApiClient.convertToType(data['endDate'], 'Date');
      if (data.hasOwnProperty('id'))
        obj.id = ApiClient.convertToType(data['id'], 'Number');
      if (data.hasOwnProperty('normPeriod'))
        obj.normPeriod = ApiClient.convertToType(data['normPeriod'], 'Number');
      if (data.hasOwnProperty('optOut'))
        obj.optOut = ApiClient.convertToType(data['optOut'], 'Boolean');
      if (data.hasOwnProperty('startDate'))
        obj.startDate = ApiClient.convertToType(data['startDate'], 'Date');
      if (data.hasOwnProperty('workdayNorm'))
        obj.workdayNorm = ApiClient.convertToType(data['workdayNorm'], 'Number');
      if (data.hasOwnProperty('workdayNormTotal'))
        obj.workdayNormTotal = ApiClient.convertToType(data['workdayNormTotal'], 'Number');
    }
    return obj;
  }
}

/**
 * If true, these settings are the default from the collective labour agreement
 * @member {Boolean} claDefault
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.claDefault = undefined;

/**
 * The employee object id
 * @member {Number} employeeObjectId
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.employeeObjectId = undefined;

/**
 * The end date of the period during which this setting is valid
 * @member {Date} endDate
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.endDate = undefined;

/**
 * The object id of the settings, required for UPDATE
 * @member {Number} id
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.id = undefined;

/**
 * The measurement period in weeks to evaluate if the workday norm is breached. Required when optOut is false
 * @member {Number} normPeriod
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.normPeriod = undefined;

/**
 * If true, the employee is opted out of the workday norm
 * @member {Boolean} optOut
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.optOut = undefined;

/**
 * The start date of the period during which this setting is valid
 * @member {Date} startDate
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.startDate = undefined;

/**
 * DEPRECATED: use workdayNormTotal instead. The maximum number of workdays the employee has to work in a week
 * @member {Number} workdayNorm
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.workdayNorm = undefined;

/**
 * The total number of workdays the employee has to work in the whole measurement period. Required when optOut is false
 * @member {Number} workdayNormTotal
 */
OnsAdministrationWorkdayNormSettingsModel.prototype.workdayNormTotal = undefined;

